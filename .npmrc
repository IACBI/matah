# Force devDependencies to install even when NODE_ENV=production.
# The build needs vite/typescript (devDeps); `include` overrides the implicit
# `omit=dev` that NODE_ENV=production sets. This makes `npm install` work on
# hosts (e.g. Render) that set NODE_ENV=production for the build step.
include=dev
